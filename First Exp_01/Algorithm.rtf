Algorithm :
{
   dx = x2 – x1
  dy = y2 – y1
  if(abs(dx) > abs(dy))
   {
     Step = dx;
   }
  else
   {
     Step = dy;
   }
 xn = dx / step;
 yn = dy / step;
 for(i=0;i<-=step;i++)
  {
    putpixel(x1,y1,RED)
    x1 = x1 + xn;
    y1 = y1 + yn;
  }

}


